# ColorfulFL

``data2`` creates random datasets.

``greedyFL2`` is the colorful dual fitting implementation where the facilities and cities are distinct groups.

``combinedFL`` is the colorful dual fitting implementation where the facilities are the cities.

``colors`` is the bibliography.
